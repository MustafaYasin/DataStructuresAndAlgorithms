# This is an input class. Do not edit.
class LinkedList:
    def __init__(self, value):
        self.value = value
        self.next = None


def removeDuplicatesFromLinkedList(linkedList):
	currentVal = linkedList
	while linkedList is not None:
		temp = currentVal.next
		while temp is not None:
			if currentVal.value == temp.value:
				temp.next = currentVal.next.next
				currentVal = temp
		
    return linkedList
